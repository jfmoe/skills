---
name: select-relevant-checks
description: 在声称验证完成、提交、push 或交接前，根据差异选择并运行最小可信的测试和质量检查，避免条件反射式运行完整仓库测试套件。
---

# 选择相关检查

针对改动行为运行一次相关本地证据。钩子应保持精简；穷尽覆盖和平台矩阵由 CI 负责。

本 Skill 只选择证据，不授权提交、push、改写历史、修改远程状态或绕过失败检查。

## 检查改动

确认仓库、当前分支或 detached 状态，以及准确范围：已提交范围、暂存、未暂存和未跟踪路径。任务涉及分支或 PR 时，核实实时 base，不要猜测。

阅读足够的代码和配置，将改动路径映射到行为、入口、所有者、消费方、生成输出和平台影响。只有路径列表不足以选择语义证据。

发生 base merge、retarget 或 rebase 后，重新计算范围，只重跑被新 base 影响的证据。

## 选择相关证据

除已配置钩子外，不存在通用本地基线。每项行为改动都需要一个能在其回归出现时失败的最小测试或专用检查；只有差异真正触及更广设计面时，才增加更广检查。

- **模块或脚本行为：** 运行所属测试文件或聚焦测试名。共享约定变化时增加相邻测试。
- **文档、决策记录、生成目录或与文档关联的注释：** 运行仓库文档检查，并根据所有者核实语义准确性。
- **面向模型、编辑器、协议、CLI、终端或用户的输出：** 运行拥有组装输出的聚焦快照或真实可运行场景。
- **包 manifest、公共导出、构建配置、worker／可执行入口或构建后运行路径：** 运行构建、相关包卫生检查和所属构建产物冒烟测试。
- **真实外部提供方行为：** 凭据和授权可用时运行相关集成或 e2e 目标；绝不打印秘密。
- **UI 行为：** 通过浏览器或等效客户端实际使用真实用户路径，覆盖主路径和重要边界状态。

不要仅因为接下来要提交、push 或交接，就手动重复已通过的检查。

## 单元覆盖率

测试选择和覆盖率选择是两件事。测试过滤器决定运行哪些测试；覆盖率必须明确这些测试应证明的受影响源码范围。

使用仓库支持的聚焦覆盖率机制。纳入所选测试会影响的每个源码文件或包。不要降低阈值、隐藏未覆盖的受影响文件，或使用“没有测试也成功”的选项制造绿色结果。

依赖图发现无法找到通过配置、动态加载、子进程、worker、构建产物、插件或外部提供方触发的行为；这些场景必须显式选择。

项目要求覆盖率时，它是必要条件，但远不充分。它只能证明代码执行过，不能证明功能按发布方式工作。

不要只为覆盖未覆盖行或分支就添加测试；先追踪其当前约定和消费方。没有存活义务的未覆盖分支属于删除候选项；应删除死代码及其专用测试，不能用新增测试制造覆盖率。

## 测试强度

优先使用真实实现，只 mock 昂贵或不确定的边界。验证外部状态，不信任系统或模型自述。当真实 loader、可执行文件、子进程、worker、发布包产物或组装应用可能不同于手工挂载单元时，应测试真实入口。

新门禁只有在目标回归会使其失败时才构成证据。有效性尚未得到证明时，应执行负向对照。

测试拥有自己的资源，并在成功、失败、重试和超时时清理。

在同进程类型边界，只测试声明接口允许的值，不测试不可能出现的敌意输入。验证和敌意输入用例应选择真实的解析器、配置、模型或工具 JSON、持久文件、worker、进程和协议边界。

## 完整本地演练

只有用户明确要求、正在诊断 CI 失败，或改动范围广到无法构造可信的更小集合时，才运行完整本地近似。以当前项目脚本和工作流文件作为清单，不要重新创造没有文档依据的聚合命令。

## 失败和报告

相关检查失败时，停止依赖该检查才能继续的动作。诊断失败或报告阻塞；不要削弱检查，也不要假定 CI 会不同。

失败测试属于需要验证的技术主张，不是权威。判断实现发生了回归，还是测试固定了有意淘汰的行为。当当前约定和消费方证据表明行为已经过时时，应随行为更新或删除测试，不能只为重新变绿而恢复过时代码。

失败似乎与环境有关时，记录精确命令、失败、平台条件和仍然通过的相关证据。证明环境差异后才能据此分类。

报告：

- 改动行为和所选证据；
- 真正运行的精确命令或人工路径；
- 通过、失败、跳过和待定结果；
- 省略每项更广检查的原因；
- 仍存在的平台、凭据、UI 或外部状态缺口。

当必要证据被跳过、不可用、待定或未运行时，不要声称“所有检查通过”。
