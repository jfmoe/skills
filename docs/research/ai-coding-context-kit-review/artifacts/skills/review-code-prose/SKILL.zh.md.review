---
name: review-code-prose
description: 编写、review、恢复、精简或审计代码库中的文字，包括 Markdown、JSDoc、注释、提示词、描述、诊断、配置注释和可见字符串，同时保留完整约定。
---

# Review 代码文字

写下足以保留约定的内容，再删除推理过程、重复和装饰。约定是调用方、被调用方、实现方、生产方或消费方依赖的义务、不变量、前置条件、后置条件或兼容承诺。

本 Skill 负责编辑判断和必要文字覆盖。文档放置和结构使用 `apply-documentation-standards`；独立的推理过程分类体系、示例和召回检索式使用 `trim-cot-leakage`。本 Skill 是指导，不是脚本。

将 `contract`、`boundary`、`shape`、`surface`、`seam`、`gate` 和 `vocabulary` 视为使用前需要检查的词，而不是禁词。先判断精确规则、API、字段集合、类型、验证、时间点、组件划分或失败状态是否能更准确地说明事实。当词语确实命名精确技术主题时予以保留。

注释说明代码无法表达的不明显约定或理由，不重复代码已经明确表达的内容。

## 范围和权限

以用户点名的文件、当前差异或另一组明确路径作为范围。如果这些都不能建立安全边界，请用户提供范围，不要自行推断要审计整个仓库。

Review 和审计任务只报告 findings，不编辑。明确要求写入、修复、恢复或精简时，才授权在指定范围内应用清楚的改动。

遵守适用的受保护路径、生成文件所有权、冻结记录和第三方源码规则。生成目录、快照和 fixture 属于派生产物：先修改所属源或场景，再重新生成。

## 保留完整命题

编辑前识别段落中的每个命题。保留每项相关的：

- 主体和动作；
- 条件、时机和顺序；
- 必须、可以、禁止等语气强度；
- 否定保证和例外；
- 所有权、副作用、失败模式和后果。

只有每个事实条款都保留且结果更清楚时，才删除形容词、重复和叙述。字数更少本身不代表改进。

在使用点保留完整局部约定：调用方或维护者在该处需要的行为、失败、所有权和后果。架构、理由、算法、历史或扩展示例链接到所属文档。每份解释只有一个真源；必要的约定事实可以在局部重复。

如果省略不明显理由可能导致误用或错误简化，应保留。否则说明后果，并链接到理由真源。

## 按文字位置确定必要覆盖

这不是单向缩短流程。当代码、类型和结构没有表达必要约定时，应新增或恢复文字。事实在局部已经明显时，不要添加注释。

- **公共 API 文档：** 调用方可见的返回差异、抛错或拒绝、副作用、所有权、时机、取消和持久性。
- **内部注释：** 非局部结构以及复杂局部不变量、竞态、所有权、安全边界和意外失败行为。删除控制流叙述和代码复述。
- **模块注释：** 角色、依赖、职责和不明显架构选择；链接到相应理由真源。
- **测试：** 只解释不明显的测试设计，例如某个 fixture、断言、平台适配、真实入口或间接观察为何必要。删除过程说明和清单。
- **操作流程：** 前置条件、必要动作、真实入口、可观察验证和精简警告。
- **README：** 消费方约定，包括配置、语义、失败、限制、扩展点和模型可见影响。链接生成目录和跨模块所有者。
- **决策记录：** 独有理由、机制、替代方案、后果、验证证据和具名覆盖缺口。已发布事实按当前状态描述。
- **事故记录：** 事故顺序、证据、因果链、影响和预防措施。
- **Skill 和 agent 指令：** 行为护栏、完成条件，以及“指导而非脚本”等明确范围限制。
- **示例和配置注释：** 访问限制、不明显的接线或加载顺序、安全立场、回放行为、例外和常见误用。不叙述可直接看见的配置项。
- **提示词和可见字符串：** 措辞属于行为。检查生成输出并运行行为验证，或说明快照不适用的原因。
- **诊断：** 不明显时，说明失败对象或路径、违反的规则和修正方法。删除内部执行过程。

保留可搜索的机制名称，以及有意义的语气、时间或否定强调。只规范装饰性强调。

需要校准时，阅读[提炼后的示例](references/examples.zh.md)。

## 工作流

1. 确认范围、写入权限、当前差异边界和适用指令。
2. 判断段落前先阅读所属代码或文档。
3. 检查用户要求的完整范围，不只看最大的文件。搜索和字数只能发现候选项，最终由语义判断决定。
4. 将每项候选标为保留、新增、精简、恢复、重组或暂缓。不要为了达到删除目标而制造改动。
5. 先更新所有者，再处理派生产物。学到新规则后重新检查同类段落。
6. 运行最小相关检查和可见字符串的行为测试。确认最终差异没有受保护路径。
7. 报告已检查范围、明确改动、有意保留、暂缓情况和实际运行的检查。

只有至少两个版本都保留每个命题、但在公认原则之间存在取舍时，才属于边界情况。只有一个保留命题的正确版本时，不属于边界情况。呈现真实替代方案及其事实或结构差异；不要为了推进工作削弱命题。
