---
name: plan-code-changes
description: 当用户要求规划、设计、实现策略或变更计划，且没有授权实施时，以只读方式探索仓库并产出决策完备的实现计划。
---

# 规划代码改动

本 Skill 只负责规划。Skill 生效期间，要求“实现”的祈使表达意味着规划实现方式，而不是执行实现。

## 先探索

通过非修改性的读取、搜索、静态分析和检查，使计划建立在真实仓库之上。不要编辑或写入文件、修改配置、运行会改写受跟踪文件的格式化或代码生成、提交，或以其他方式执行计划。现有函数和模式适用时，优先使用，避免增加新机制。

通过检查自行查明可发现事实。只有用户拥有决定权，或检查无法消除实质性歧义时，才向用户提问。当仓库可以给出答案时，不要询问代码位置或当前行为。

追踪受影响的入口、所有者、消费方、数据流、测试、文档和适用指令。只有证据无法决定时才记录假设。

## 使计划决策完备

说明目标和可验证的成功标准。按子系统组织实现改动，并根据依赖关系排序。

明确：

- 可能修改的文件或模块及其原因；
- 公共 API、schema、持久化、配置和数据流变化；
- 所有权和生命周期影响；
- 兼容或迁移决定；
- 边界情况和失败模式；
- 用于证明结果的最小测试、集成路径、快照或人工验收检查；
- 明确假设、用户拥有的决定、风险和非目标。

计划应足够精简，便于 review；也应足够具体，使另一位工程师无需再作设计决策即可实施。

## 完成条件

返回完整计划并保持只读。如果确实无法获得某项必要事实，请说明缺失证据及其阻塞的精确决定。只有获得另一次实施授权后才能开始修改。
