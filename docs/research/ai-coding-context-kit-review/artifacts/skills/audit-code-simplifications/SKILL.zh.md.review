---
name: audit-code-simplifications
description: 当用户要求删除无用或重复行为、减少推测性设计、折叠不必要抽象，或用维护良好的依赖替换手写基础设施时，在现有代码库中寻找证据充分的简化候选项。
---

# 审计代码简化机会

将宽泛的“找出可以简化的内容”转化为少量证据充分、能够删除、折叠或降级现有设计面的候选项。本 Skill 是指导，不是检查清单：沿着代码调查，持续运用判断，宁可提交少量证据扎实的候选项，也不要堆积缺乏依据的猜测。

## 确立当前约定

判断设计前，先阅读适用的仓库指令、架构、当前决策记录、测试和公开文档。对已有记录的 seam 和有意保留的替代实现，默认视为刻意设计，除非新证据能够推翻其理由。

每项简化都必须说明现有内容、所属所有者、当前消费方、承担的义务，以及改动后真正消失的内容。

## 强候选项

强候选项会删除、折叠或降级真实存在的内容，并有明确证据表明当前设计成本高于收益：

- 公共方法、事件、配置开关、注册表通知、辅助函数、包、持久事件或测试产物没有生产消费方。
- 只有测试或文档消费某项行为，而该行为并不承担关键约定。
- 两种表示重复保存同一事实。
- 某个 seam 要求每种实现支持方法，但没有消费方使用。
- 某个包或模块只包含测试、演示或支持代码，却带来发布或依赖开销。
- 某项功能为没有产品所有者或当前消费方的情况实现推测性通用能力。
- 某个不变量、回滚路径、预期输出或特殊测试只用于保护未使用 API。
- 某个未覆盖行或分支没有当前约定或生产消费方；覆盖率失败是删除证据，不是机械补测试的理由。
- 验证、fallback 或敌意输入测试保护的是同进程类型接口下不可能出现的值，而不是真实的解析器、配置、模型或工具 JSON、持久文件、worker、进程或协议边界。
- 手写代码重复实现维护良好的依赖或平台内置能力，替换后可以删除实现及其专用测试。
- 简化后的行为可以略有差异，但新行为仍然合理且更容易说明。

删除拼写错误、只说“这里看起来复杂”，或在没有消费方分析的情况下删除有意支持的替代实现，都不属于强候选项。

## 广泛调查

从最大的生产代码和生命周期设计面入手，不要停留在未使用符号工具的输出。跨接口、实现、配置、动态加载、运行时字符串、文档、测试、快照和脚本搜索。

对每次防御性复制、冻结、验证和回调捕获，都要说明值来自哪里、接下来由谁拥有。同进程的类型化调用通常借用类型化值；解析器、配置、队列、模型或工具 JSON、持久文件、worker、进程和协议解码器负责拥有或验证自己的数据。

对异步代码，绘制所有权图，并将每个 sentinel、就绪 promise、取消路径、disposer 和状态标志映射到不同所有者或状态转换。多个机制重复表示同一存活或结算事实时，可以考虑合并为一个事务或生命周期控制器。当独立机制保护发布回滚、回调隔离、终态仲裁、worker／进程所有权或 dispose 完全停稳时，应予保留。

## 手写代码与依赖

当引入依赖能够删除自有代码和测试时，它是有效的简化手段。

- 说明依赖覆盖的精确手写设计面。依赖未覆盖的残余语义会降低替换收益。
- 检查维护状态、采用度、安全状况、许可证、平台支持和传递依赖规模。受支持运行时已提供相应能力时，优先使用平台内置实现。
- 计算净删除量：实现、专用测试和文档的删除量，减去仍需保留的胶水代码。
- 只把相同复杂度搬进包装层不构成收益。
- 不能仅因为存在某个包就推翻已记录的架构决定；替代方案必须优于当前理由。

## 证明或否决每个候选项

编写结论前先对消费方分类：

- **生产**：运行时源码、配置、loader、公共 API 和已发布入口。
- **非生产**：测试、文档、决策记录、快照、生成输出和注释。
- **模糊**：可能属于产品冒烟或发布路径的示例和脚本；分类前必须检查。

搜索精确符号、事件、包、配置键、方法和协议字符串，再阅读所有实质性调用点。静态未使用代码工具适合发现线索，但不理解动态名称、公共接口、配置或产品入口。

现有测试描述行为，不能证明该行为仍然是必需的。当约定和消费方证据表明行为已经过时时，应同步修改或删除行为及其专用测试。不要只为维持过时测试为绿色而保留代码，也不要只为覆盖死代码而添加测试。

遇到以下情况时否决或降低候选项优先级：

- 存在生产调用方，改动属于功能决定而不是清理；
- 当前约定或踩坑形成的防御规则已有充分理由，而新证据不足以推翻；
- 删除会制造无关改动，却没有减少公共 API 或必需行为；
- 想法正确，但范围过小，不值得写成长期设计提案。

## 报告

对每项接受的候选项说明：

- 当前所有者和受影响设计面；
- 生产、非生产和模糊消费方证据；
- 精确的删除、折叠、降级或替换内容；
- 放弃的行为或能力；
- 对代码、测试、文档、依赖和运维的净影响；
- 风险和重新引入条件；
- 可观察的验收标准及证明它们的最小检查。

同时报告具有代表性的否决项及其未达标原因。不要为了凑数量制造候选项。
