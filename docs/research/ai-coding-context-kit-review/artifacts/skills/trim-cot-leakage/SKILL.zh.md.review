---
name: trim-cot-leakage
description: 审计或修复读起来像推理过程泄漏的文字，包括失效的设计会话引用、审计项编号、未提交草稿章节、变更叙述、堆叠或 review 视角、面向评审人的辩解、控制流叙述、测试过程和带保留语气的规划残留。
---

# 清理思维链泄漏

思维链泄漏是指文字采用写作会话视角，而不是仓库视角：它引用只有该会话能看到的产物，叙述改动过程而不是当前状态，或继续和已经离开的评审人争论。当段落包含事实条款时，修复绝不能只是删除：先把每项事实改写成能够在当前仓库状态独立成立的表述，再删除周围的推理过程。完全不含事实的内容，例如审计编号或控制流叙述，应直接删除。

本 Skill 应用的完整命题规则由 `review-code-prose` 负责。本 Skill 是指导，不是脚本。

## 一条判断

对每个可疑段落都问：**只看到当前仓库状态、无法访问任何会话 transcript、review 线程或未提交草稿的读者，能否解析每项引用并验证每项主张？** 如果不能，从仓库视角重述仍然成立的事实，并删除其余内容。如果可以，无论听起来多么历史化，它都不属于泄漏；但“可解析”只满足本 Skill 的门槛：在 README、文档和 JSDoc 等描述当前状态的位置，一段可解析的变更故事仍然属于变更叙述，第 3 类会把它导向允许的归属位置。

## 分类体系

1. **失效的设计会话引用**——`(decision 7)`、`(audit C2)`、`design §4.7`、`plan §1.4`、`T4`、`W3`、`P-I` 等阶段标签、“the design ledger”和“(B ruling)”。如果决定有已提交的所有者，应按名称和路径引用；否则删除引用，并把事实条款改写成能够独立成立的句子。
2. **堆叠和 PR 视角**——“a later PR in this stack”“this PR adds”“the previous commit”。说明已发布机制或扩展点；暂缓工作移入有所有者的 TODO 标记或 issue 引用。
3. **变更叙述和版本戳**——“used to”“no longer”“the old X”，以及“v1”“this cut”“today”或用“now”对比过去状态等索引式版本戳。说明当前行为。已修复回归改写为现在时反事实，例如“without X, Y happens”，不要写成“used to Y”这类仓库历史。
4. **Review 过程**——“Rejected in review”“the reviewer confirmed”、草稿序号“v5 of this note”和 review 轮次归因。把仍然有效的决定和理由写成普通事实；删除谁在何时说了什么。
5. **面向评审人的辩解**——“the cast is safe—it simply…”“this is correct because…”。为自身正确性辩护的注释是在回应评审人，不是在帮助维护者。说明使代码安全的不变量；代码已经表达时删除注释。
6. **复述和推导 transcript**——“first we X, then we Y”等控制流叙述、测试过程和对明显分支的证明。直接删除；只保留不明显的约定或不变量。
7. **保留语气和规划残留**——“probably fine for now”“should be enough”以及没有所有者的暂缓事项。提升为有所有者的 TODO／FIXME，或说明实际边界；删除保留语气。
8. **写作语言残留**——整体语言一致的文字中夹杂未翻译的工作语言片段或私有草稿分隔符。翻译或删除。

## 不属于泄漏的内容

没有判断辅助的引用清理会同时误删长期引用和保留失效引用。应按原意应用以下保留规则；[示例](references/examples.zh.md)为每类情况提供校准：

- **Issue 引用**——`#1470`、`TODO(name):` 和“issue #N owns the follow-up”在当前仓库状态可解析；任何位置都可以保留。
- **决策记录和事故记录中的已合并 PR／issue 引用**——当这些文档类型负责记录变更历史时，它们属于允许的证据。
- **抑制规则理由**——linter disable 理由、覆盖率 ignore 理由和空 catch 解释属于必要文字；理由错误时应修正，不能删除。
- **现在时反事实回归固定语句**——“without X, Y happens”“a naive X would…”。
- **测量边界**——用于校准常量的“measured: 512 nests ≈ 0.15s”；来源词“measured”承担实际信息。
- **运行时 old/new 状态**——“the old connection drains before the new one accepts”描述运行时生命周期，不是仓库历史。
- **记录中变更故事章节的历史阶段名**——“the first cut shipped X”在该位置可能安全；“this cut”等索引式版本戳仍然不安全。
- **按设计在仓库外解析的外部引用**——标准章节和具名设计产物。章节号问题针对未提交内部草稿，不针对可解析外部标准或已提交文档。
- **项目口吻和文体形式**——项目口吻中的“we”和“Alternatives considered”章节本身不构成泄漏。

## 工作流

1. 建立明确范围和适用的受保护路径。除非所属工作流明确授权，否则不要编辑 vendor 或第三方源码、冻结记录、已录制 fixture 或快照。
2. 先只读审计。运行[召回检索式](references/recall-batteries.zh.md)，再从语义上判断每个命中。检索式只是探针，不是定义。还要在没有预设模式的情况下阅读范围内文字最密集的位置。
3. 先修所有者：生成输出 → 来源或生成器；复制的 API 文本 → 所属声明；配对文档 → 两侧；模型可见字符串 → 有行为验证以及所属快照或 e2e 场景的改动。
4. 删除前，根据 `review-code-prose` 列出段落命题，并检查[过度修正陷阱](references/examples.zh.md#过度修正陷阱)。不要把义务改成认可、把假设提升为已发布行为、删除真实事实或丢失来源信息。
5. 重新运行检索式，预期只剩允许保留项和校准材料。确认每个剩余引用都能在当前仓库状态解析，并运行所有受影响设计面的检查。
6. 报告已检查范围、修复、允许保留项、未解决情况和实际运行的检查。
