---
name: apply-documentation-standards
description: 编写、移动、审计或简化教程、参考文档、README、API 文档、决策记录或生成文档时，决定文档归属并审查其语义结构；不执行格式和样式指南。
---

# 应用文档标准

本标准关注语义，不关注排版。判断文档拥有什么内容、读者如何使用，以及哪些事实应放在其他位置。本 Skill 有意不规定换行、字数、标题样式、双语布局、标点或其他格式政策。

完整约定和编辑判断使用 `review-code-prose`。问题属于推理过程泄漏时使用 `trim-cot-leakage`。

## 先审查结构，再审查文字

对范围内每篇面向人的文档依次执行：

1. 确定文档在仓库和导航树中的位置。说明其自身主题，并识别直接子项。
2. 确定允许的细节层级。完整说明文档自身主题；对直接子项只概述用途、职责和高层行为；更深层解释移动到所属子项并通过链接引用。
3. 根据预期用途分类：
   - **教程**通过有序工作引导读者达到可观察结果，并且每一步只引入当时需要的概念；
   - **参考文档**支持在明确范围内查找信息，描述当前行为，不要求按顺序阅读。
4. 对教程，识别读者起点和每个概念的前置知识。先建立前置概念，再介绍依赖概念；可选高级内容移到后面。
5. 将大量教程和参考内容拆开。少量次要形式可以保留在名称清楚的章节中。

文档类型不会扩大其范围。参考文档只能对自身主题做到穷尽。测试机制、fixture 和测试框架应放在最低层所有者处；上层文档链接到那里。

## 每项事实只有一个真源

每项事实只有一个归属位置：负责维护它的文档。其他位置只链接，不重复表述。

使用仓库实际层级，但保留以下语义角色：

- 长期指令只包含 agent 在上下文中始终需要的规则；
- 架构文档说明组合、所有权和扩展点；
- 子系统或 API 参考定义类型和当前语义；
- 决策记录保存理由、替代方案、后果和必要验证；
- 事故记录保存时间线和因果证据；
- 操作流程引导执行动作并进行可观察验证；
- 包或模块参考说明消费方约定；
- 生成参考从权威真源重新生成。

移动文档前先找到所有入链，并原子更新整个移动。绝不手工编辑生成文档；应修改其来源或生成器。

## 审计语义赘余

从语义上查找以下问题；搜索模式只能帮助发现：

- 同一规则出现在多个真源；
- 叙述仓库改动历史，而不是当前行为；
- 重复仓库树或 manifest 已能回答的实现状态；
- 手写复述目录、清单、源码类型、测试或生成数据；
- 推理过程、控制流叙述、测试过程、review 过程和被否决的局部替代方案；
- 同一理由在多个兄弟位置重复，而不是只放在所属能力处；
- 上层文档包含属于下层所有者的实现细节；
- 长期文档引用无法解析的会话、草稿、review 轮次或临时计划。

保留每条关键规则和约定。不要为了移动无用推理而新建另一份解释。

如果删除文字会改变承诺行为，而不仅是改变解释，应将其视为产品或设计变化，并进入项目的决策流程。

## 当前状态准确性

根据所属代码、配置、生成源、公共接口或已观察行为核实每项主张。路径、符号、默认值、机制和失败行为发生变化时，在同一改动中更新陈旧事实。

直接改写陈旧事实，不追加变更历史。长期决定发生反转时，应创建新决策记录或明确 supersession，不能静默重写原理由。

## 验证

运行目标仓库相关的文档检查、链接检查、生成器和行为测试。自动检查只能证明结构和新鲜度；语义准确性、完整性、放置和清晰度仍由 review 负责。

报告已检查范围、移动或所有权变化、有意保留项、删除的重复或叙述、已核实来源和实际运行的检查。
