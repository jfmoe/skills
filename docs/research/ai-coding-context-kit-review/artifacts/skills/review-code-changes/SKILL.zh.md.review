---
name: review-code-changes
description: 当用户要求语义代码 review 时，检查 PR、分支、提交范围或工作区差异中的正确性、生命周期、安全、约定、文档和测试证据缺陷。
---

# Review 代码改动

本 Skill 是指导，不是完整检查清单。阅读差异前，先核实实时 base 和准确 head，或明确工作区边界。随后阅读足够的周边代码、适用指令、测试、文档和决策记录，理解设计。改动范围报告只能标出路径和受影响层，不能替代语义 review。

正确性、生命周期、安全、数据丢失和必需行为损坏优先于样式。一份只有一项证据充分阻塞问题的简短 review，优于一长串细枝末节。

## 确立 review 约定

明确：

- 用户要求的行为或原始规范；
- 接受 review 的精确 base、head、暂存、未暂存和未跟踪范围；
- 适用的根目录和子目录指令；
- 当前架构和长期决策；
- 改动影响的公共接口及其所有当前消费方；
- 已由机械门禁执行的检查，避免重复报告已通过门禁覆盖的问题。

发生 retarget、rebase 或 merge 后，重新确认 base 并再次发现范围。

## 阻塞要求

1. **意图和接口约定一致。** 追踪每个变更接口的两端，确认错误、取消、所有权、时机、dispose、持久化和兼容行为符合要求。
2. **新增文字接受语义 review。** 严格审查所有新增或修改的 Markdown、JSDoc、注释、提示词、描述、诊断和可见字符串。自动检查不能证明准确性、放置、完整性或编辑质量。
3. **文档与代码一致。** 配置、默认值、错误、协议字段、事件和公共行为应在同一改动中更新所属 README、API 文档和不明显的 JSDoc。
4. **注册项和资源正确清理。** 每个新监听器、注册表贡献、worker、进程、定时器、临时路径和订阅都必须有所有者及可观察的 dispose。
5. **存在必要证据。** 核实所选测试和检查覆盖改动行为，其余穷尽矩阵由 CI 负责。
6. **规则在操作中执行。** 沿每条拒绝路径追踪到真正执行操作的位置；直接或替代调用方不能借不同 schema、提示词、facade、包装层或监听器顺序绕过政策。

## 人工检查

- **生命周期和并发：** 检查发布前竞态、await 期间取消、独立结果报告、回调隔离、重新进入前的所有权、完整解绑清理和 dispose 完全停稳。
- **能力和消费方匹配：** 追踪每个当前消费方。标记泄漏进通用接口的消费方专用行为，以及只有单一内部调用方却扩展通用公共 API 的方法。
- **范围、所有权和必要性：** 将每项抽象、状态机、选项、防御性复制和兼容路径映射到当前约定、生产消费方和所有者。质疑无关功能和推测性通用设计。
- **配置和公共选择：** 默认值、操作集合、格式和外来概念需要当前消费方证据或相关先例；否则要求明确选择或暂缓。
- **模型视角：** 检查模型实际收到的提示词、工具 schema、结果、诊断和运行时上下文。删除任务之外的实现概念。
- **借用和派生状态：** 判断每个保留值是借用还是拥有。把通知、缓存、提示词、UI 回显、回放和查询视图追踪到权威真源和已记录的成功点。
- **完整边界：** 找到完整输出或保留结果的所有者，包括包装层和元数据。测试极小值、精确边界、超大单分片，以及字节边界下的多字节文本。
- **真实入口：** 当手工挂载单元可能隐藏故障时，要求通过实际 loader、可执行文件、worker、子进程、发布包产物或组装应用进行测试。
- **测试强度：** 断言必须在目标回归出现时失败，并检查外部状态、事件、日志、清理或用户可见行为，不能信任实现或模型自述。
- **测试必要性和边界真实性：** 现有测试描述行为，但不能证明该行为仍然必需。当前约定和消费方证据表明行为已经过时时，应一起 review 实现和测试，不能只为测试套件保持绿色而保留过时代码。没有存活义务的未覆盖代码属于删除候选项；敌意输入测试只应放在真实的不可信边界，不能放在同进程类型接口。
- **负向对照：** 新门禁的有效性尚未得到证明时，通过真实运行器证明目标无效情况确实失败。
- **Transcript 改动：** 编辑器、协议、模型或用户可见改动更新所属快照，或说明快照不适用的原因。预期输出差异属于行为变化，不是格式噪声。
- **生成和受保护内容：** 先修改所有者，再生成投影；不要规范化冻结记录或随意编辑受保护源码。

## Findings

说明缺陷、位置、影响和证据。局部缺陷放在最紧的相关范围；跨架构、范围或综合问题使用 review 级 finding。区分阻塞项和建议，并省略已由绿色门禁执行的问题。

接收 review 时，从技术上核实每项主张，并决定修复、反驳或暂缓。评审人、自动适配器或委派 agent 的报告是待检查证据，不是权威。修复应落在引入缺陷的改动上，然后重新验证实际结果树。
