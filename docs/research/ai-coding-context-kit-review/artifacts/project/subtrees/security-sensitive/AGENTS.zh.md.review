# 安全敏感代码候选指令

- 默认拒绝。当授权、隔离、验证或执行机制不可用或不符合约定时，应拒绝操作，不能改用未受约束的 fallback。
- 只有针对该具体操作的明确批准结果才构成授权。缺失、取消、拒绝、抛错或格式异常的决定都不授予权限。
- 在真正执行副作用的操作中落实政策，并通过直接和替代调用方测试拒绝路径。
- 携带凭据的请求禁止自动跟随重定向。测试必须证明重定向目标没有收到请求。
- 从子进程环境中移除秘密。临时文件或 spill 文件使用私有目录、随机名称和仅所有者可访问的独占创建方式。
- 可能是符号链接或 junction 的路径按 link 处理：检查时不跟随目标，并 unlink 该链接，不能递归删除进入目标。
- 受保护或 vendor 源码的改动必须与明确的所属元数据和工作流绑定。
