# 面向 Agent 产品的候选指令

- 从模型任务视角编写提示词、工具 schema、结果、诊断和运行时上下文。除非模型必须据此行动，否则不要暴露 UI、传输或实现词汇。
- 进入模型请求的任何内容都必须能从权威会话记录或请求记录中重建。
- 将文字视为行为。稳定的模型可见文本逐字固定；动态组装通过真实可运行路径、快照或 e2e 场景验证。
- 在执行器或所属操作中落实权限和拒绝规则，不能只依赖提示词、schema、包装层或展示层。
- 业务结果归执行逻辑所有。渲染和展示只能转换模型或用户看到的形式。
- 测试组装后的请求和可观察结果。只对某个提示词片段做单元测试，不能证明发布组合正确。
