# 生命周期和异步代码候选指令

- 相互独立的结果应分别报告；不要把超时、信号、取消或退出事实隐藏在另一结果分支后面。
- 不要把异步状态当成单次请求的结果。明确自己拥有的时间区间，并观察真正使其结算的事件或 promise。
- 一次异步操作默认由一个生命周期控制器或事务表示；只有状态具有真正独立的所有者或结算点时才拆分。
- dispose 必须等待完全停稳：先阻止新通知，再请求取消，等待所属子任务退出，最后结束清理。
- 在分发器中隔离用户回调异常，避免单个订阅方拒绝核心生命周期任务或阻断后续订阅方。
- 只在成功后发布状态；测试应保留并验证回滚、回调隔离、取消和销毁顺序。
