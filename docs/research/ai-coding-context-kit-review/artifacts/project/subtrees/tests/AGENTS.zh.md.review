# 测试候选指令

- 每项行为改动都需要一个能在其回归出现时失败的最小测试或专用检查。覆盖率只能证明代码执行过，不能证明功能按发布方式工作。
- 测试描述行为，不证明正确性，也不会使某项行为天然成为必需。当当前约定和消费方证据表明行为已经过时时，应同步修改或删除行为及其测试。不要只为维持过时测试为绿色而保留代码。
- 不要只为覆盖未覆盖行或分支就添加测试；先确认其当前约定和消费方。死代码及其专用测试应直接删除，不能用新增测试制造覆盖率。
- 在同进程类型边界，只测试声明接口允许的值，不测试不可能出现的敌意输入。验证和敌意输入测试应放在真实的解析器、配置、模型或工具 JSON、持久文件、worker、进程和协议边界。
- 优先使用真实实现。只 mock 外部模型、网络或时钟等昂贵或不确定的边界；下游行为保持真实。
- 验证外部世界，不信任系统自述。重新读取文件、查询外部状态、检查事件或直接观察 dispose 结果。
- 当加载、打包、子进程、worker、CLI 或组装组合可能不同于手工挂载单元时，测试真实入口。
- 门禁只有在目标回归会使其失败时才真正起作用。新规则或有效性不确定时，应证明负向对照。
- 测试拥有其资源：在测试中创建，并在成功、失败、重试和超时时清理。
- 证据与行为面匹配：单元行为、面向模型或用户输出的组装快照、发布路径的构建产物冒烟测试，以及提供方行为的真实接口测试。
- 不要为文本文档或提示词措辞编写单元测试。应测试代码行为、生成结构或可观察的模型／用户输出。
