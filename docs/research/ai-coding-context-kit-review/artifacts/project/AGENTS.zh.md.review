# 候选项目级指令

## 范围和所有权

- 修改行为前，先阅读仓库真实的架构、脚本、配置和适用的子目录指令。检查实际接口，不要根据名称、示例或记忆中的 schema 推断完整接口。
- 每项抽象、状态机、选项、兼容路径和公共操作都必须对应当前约定或生产消费方。行为应留在其所属模块或服务中。
- 除非项目规则另有明确规定，实验代码遵守与稳定代码相同的工程、安全、文档、生命周期和测试义务。

## 改动约定

- 同步更新行为、测试、公开文档和不明显的 API 约定。注释解释代码无法表达的隐藏义务、所有权、时机、失败或理由；不叙述控制流、测试过程或 review 历史。
- 先修改规范源，再生成投影。除非所属工作流明确授权，否则不要编辑生成文件、vendor 源码、冻结记录或其他受保护路径。
- 在作出决定的操作中执行规则。当其他调用方可以绕过时，提示词过滤、schema、包装层、监听器顺序和 UI 隐藏都不构成执行。
- 只在操作提交点之后发布状态。字节、token、条目数和时间边界必须作用于包含包装层和元数据的完整输出或保留结果。

## 证据

- 根据改动行为选择检查。局部信号由聚焦测试负责；穷尽覆盖和平台矩阵由 CI 负责。
- 现有测试描述当前行为，不能证明该行为仍然是必需的。当前约定和消费方证据表明某项行为已经过时时，应同步修改或删除行为及其测试。为未覆盖分支补测试前，先判断该分支仍然存活还是已经属于死代码。
- 信任已声明的同进程类型接口。敌意输入验证和测试应放在真实的解析器、配置、模型或工具 JSON、持久文件、worker、进程和协议边界。
- 将面向模型、协议、CLI、编辑器和用户的文字视为行为。稳定文本逐字固定；动态组装行为通过快照或 e2e 测试覆盖。
- 将机械可判定的不变量放入实际执行的钩子或 CI 门禁。语义判断留给 review，不要用脆弱的文本匹配编码文案政策。

## 条件工作流

- 规划实现时使用 `plan-code-changes`。
- 审计简化机会时使用 `audit-code-simplifications`。
- 进行改动语义 review 时使用 `review-code-changes`。
- 选择测试和检查时使用 `select-relevant-checks`。
- 决定文档放置位置时使用 `apply-documentation-standards`。
- 审查代码文字时使用 `review-code-prose`；问题属于推理过程泄漏时使用 `trim-cot-leakage`。
