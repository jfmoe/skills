# Sources for `README.md`

This README is an original packaging, review-state, and promotion index for the portable artifacts created in this bundle. It copies no DeepSeek prompt wording.

- The fixed upstream repository and commit are inventoried in [`SOURCE_CATALOG.md`](SOURCE_CATALOG.md).
- Skill and AGENTS names come from the adjacent disabled `.review` copies; their intended live names and destinations come from the candidate config, each fork's `meta.yaml`, and this repository's instruction/skill layout rules.
- Project artifact names and adoption commands come from the files under `project/` and their source sidecars.
- The separation among user defaults, project rules, subtree rules, Skills, and mechanical checks—and the decision to keep every candidate inside one inactive review folder—is the packaging judgment requested by the user.
- The verbatim-first extraction rule records the user's review decision: portable upstream wording stays unchanged unless a product or repository dependency requires adaptation.
